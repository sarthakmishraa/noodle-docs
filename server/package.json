{
  "name": "noodle-docs-be",
  "version": "1.0.0",
  "description": "Noodle Docs backend with websocket ws instead of socket.io",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc",
    "start": "node --env-file=.env ./dist/index.js",
    "dev": "npx tsc && node --env-file=.env ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sarthak Mishra",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.3.0",
    "@types/ws": "^8.5.12"
  }
}
